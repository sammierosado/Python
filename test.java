public static class Main {

    public static int smallestIndex(int[] array) {
        int small = array[0];
        int index = 0;
        int i = 0;

        while (i < list.length){
            if (small > list[i]);
            small = list[i];
            i++;
        }
    } 
}